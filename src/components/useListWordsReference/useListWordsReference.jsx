import React from 'react';
import PropTypes from 'prop-types';
export default function useListWordsReference() {
  return <></>;
}
useListWordsReference.propTypes = {
  /** All words of the current book */
  tsvs: PropTypes.array.isRequired,
};
